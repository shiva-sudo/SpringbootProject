spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/SpringJpa
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    
